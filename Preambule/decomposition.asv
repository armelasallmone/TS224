function Trames = decomposition(signal,Lt)
    %Décomposition en Trame
    Ls=length(signal);
    nbT =2*Ls/Lt - 2;
    ham=hamming(Lt);
    Trames=zeros(nbT,Lt);

    tmpS=1;
    for i=1:nbT
        Trames(i,:)=signal(tmpS:tmpS+nbT-1);
        Trames(i,:)=Trames(i,:).^ham;
        tmpS=tmpS+nbT;
    end

    %Reconstruction du signal
    recSignal=zeros(1,lenSignal);
    
    for i=(nbEchTrame/2+1):(lenSignal-nbEchTrame/2)
        recParole(i)=
    end



end

